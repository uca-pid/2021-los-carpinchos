import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
	palette: {
		primary: {
			main: "#6c63ff",
		},
	},
	typography: {
		fontFamily: "GillSans",
	},
});

export default theme;
